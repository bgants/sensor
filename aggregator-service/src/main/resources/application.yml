server:
  port: 8080
spring:
  datasource:
    url: jdbc:postgresql://tethys:5432/sensordb
    username: sensor
    password:
  jpa:
    hibernate:
      ddl-auto: update
    temp:
      use_jdbc_metadata_defaults: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        jdbc:
          lob:
            non_contextual_creation: true
